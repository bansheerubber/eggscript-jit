%a = 5;
%b = 1;
%c = 3;
%d = 5;
%e = 5;

function a(): number {
	return 5;
}

function b(): number {
	return 1;
}

function c(): number {
	return 3;
}

function d(): number {
	return 2;
}

function e(): number {
	return 4;
}

%test1 = (%a && %b && %c && %d && %e) + (5 * 2 && %c && 1 + 6) + (1 && 0 && 5);
printNumber(%test1);

%test2: number = a() && b() && c() && d() && e();
printNumber(%test2);

%test3 = (%a || %b || %c || %d || %e) + (5 * 2 || %c || 1 + 6) + (1 || 0 || 5);
printNumber(%test3);

%test4: number = a() || b() || c() || d() || e();
printNumber(%test4);

%test5 = (%a && %b) || %c;
printNumber(%test5);

%test6 = 0 || (%a && %b);
printNumber(%test6);

%test7 = (%a || %b) && %c;
printNumber(%test7);

%test8 = 1 && (%a || %b);
printNumber(%test8);
